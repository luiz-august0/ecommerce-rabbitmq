services:
  db.florinda:
    image: mysql:9
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: senha123
    ports:
      - '3306:3306'
    volumes:
      - mysql.florinda:/var/lib/mysql
volumes:
  mysql.florinda: